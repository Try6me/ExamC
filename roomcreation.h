#include <stdlib.h>
#include <math.h>
#include <stdio.h>



//int main(void)
void createroom()
{       
        char i = "V";
        char tab[0]={"V"};
        printf("Veuillez sélectionner la taille de la salle : ");
        int nombre = 625 ;
        int r = 0;
        r = (int) sqrt(nombre);
        int x=r;
        int y=r;
        if (r==r)
                printf("%d\n",r);
        else
                return NULL;
        tab[][]=
}

        /*for(i=0;i<x;i++){
                for(i=0;i<y;i++){
                        tab[i]="V";
                }
        }
}       /*
        if (nombre != NULL)
        {
                printf("ok");
        }
        else
                printf("no");
        return 0;*/
